import { ArrowRight, CalendarDays, ChevronRight, Home, MapPin, MessageCircle, Minus, Plus, Send, ShieldCheck, Sparkles, X } from "lucide-react";
import { useState } from "react";

const QUICK_REQUESTS = ["Deep cleaning", "Standard cleaning", "Move in / out", "Recurring clean"];

function RoomControl({ label, value, minimum, maximum, onChange }: { label: string; value: number; minimum: number; maximum: number; onChange: (value: number) => void }) {
  return <div className="rounded-2xl border border-stone-200 bg-white p-4 shadow-sm"><div className="flex items-center gap-2 text-sm font-bold text-stone-800"><Home className="h-4 w-4 text-[#ff684c]" />{label}</div><div className="mt-3 flex items-center justify-between rounded-xl bg-stone-50 p-1.5"><button type="button" aria-label={`Decrease ${label}`} disabled={value === minimum} onClick={() => onChange(value - 1)} className="grid h-9 w-9 place-items-center rounded-lg bg-white text-[#e95d44] shadow-sm disabled:opacity-30"><Minus className="h-4 w-4" /></button><strong className="text-lg text-stone-800">{value}</strong><button type="button" aria-label={`Increase ${label}`} disabled={value === maximum} onClick={() => onChange(value + 1)} className="grid h-9 w-9 place-items-center rounded-lg bg-[#ff684c] text-white shadow-sm disabled:opacity-30"><Plus className="h-4 w-4" /></button></div></div>;
}

export default function GuideBookingMock() {
  const [panelOpen, setPanelOpen] = useState(() => typeof window !== "undefined" && new URLSearchParams(window.location.search).get("panel") === "open");
  const [prompt, setPrompt] = useState("");
  const [asked, setAsked] = useState(false);
  const [bedrooms, setBedrooms] = useState(2);
  const [bathrooms, setBathrooms] = useState(2);

  const ask = () => { if (prompt.trim()) setAsked(true); };

  return <main className="min-h-screen overflow-hidden bg-[#f7f3ef] text-stone-900"><div className="pointer-events-none fixed inset-0 bg-[radial-gradient(circle_at_18%_15%,rgba(255,206,193,.55),transparent_28%),radial-gradient(circle_at_88%_82%,rgba(255,232,226,.8),transparent_28%)]" /><section className="relative mx-auto flex min-h-screen max-w-[1500px] items-center justify-center px-5 py-10 lg:px-14"><div className={`absolute inset-y-0 left-0 hidden w-[62%] bg-white/70 transition lg:block ${panelOpen ? "opacity-100" : "opacity-0"}`} /><div className="relative grid w-full max-w-[1300px] gap-6 lg:grid-cols-[minmax(0,1fr)_470px]"><div className={`hidden rounded-[32px] border border-white/80 bg-white/70 p-10 shadow-[0_22px_80px_rgba(65,44,31,.10)] backdrop-blur-sm lg:block ${panelOpen ? "opacity-100" : "opacity-0"}`}><span className="text-xs font-extrabold tracking-[.18em] text-[#e85e45]">MAIDS IN BLACK</span><h1 className="mt-3 max-w-md text-4xl font-black tracking-tight text-stone-900">A cleaner home, without the back-and-forth.</h1><p className="mt-4 max-w-lg text-base leading-7 text-stone-500">Madison answers questions first. When you are ready, the booking panel handles the complete structured booking in one place.</p><div className="mt-8 grid max-w-xl grid-cols-3 gap-3">{["Choose your cleaning", "Pick a time", "Reserve securely"].map((item, index) => <div key={item} className="rounded-2xl border border-stone-200 bg-white p-4"><span className="grid h-7 w-7 place-items-center rounded-full bg-[#fff0ec] text-xs font-black text-[#d95740]">{index + 1}</span><strong className="mt-3 block text-sm">{item}</strong></div>)}</div></div>
      <section className="relative flex min-h-[720px] flex-col overflow-hidden rounded-[32px] border border-stone-200 bg-[#fffdfb] shadow-[0_28px_80px_rgba(56,37,28,.18)]"><header className="flex items-center justify-between border-b border-stone-100 px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#fff0ec] text-[#ff684c]"><Sparkles className="h-5 w-5" /></span><div><strong className="block text-base">Madison</strong><span className="flex items-center gap-1 text-xs text-stone-500"><i className="h-2 w-2 rounded-full bg-emerald-500" />Here to help, not rush</span></div></div><button type="button" onClick={() => { setAsked(false); setPrompt(""); }} className="rounded-xl border border-stone-200 px-3 py-2 text-xs font-bold text-stone-500">Start over</button></header>
        <div className="flex-1 overflow-y-auto px-5 py-6"><span className="text-[11px] font-extrabold tracking-[.18em] text-[#e85e45]">MAIDS IN BLACK GUIDE</span><h2 className="mt-2 text-[30px] font-black leading-tight tracking-tight">What can we help with?</h2><p className="mt-3 text-sm leading-6 text-stone-500">Ask a question or describe the cleaning you need. Your booking is one click away.</p>{asked ? <div className="mt-7 space-y-4"><div className="ml-auto max-w-[84%] rounded-[20px_20px_6px_20px] bg-[#fff0ec] px-4 py-3 text-sm leading-6 text-stone-800">{prompt}</div><div className="flex gap-2"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-xl bg-[#fff0ec] text-[#ff684c]"><Sparkles className="h-3.5 w-3.5" /></span><div className="rounded-[20px_20px_20px_6px] border border-stone-200 bg-white px-4 py-3 text-sm leading-6 text-stone-700">I can help with services, timing, and how booking works. When you are ready, the booking panel is right below.</div></div></div> : <><div className="mt-7 rounded-3xl border border-stone-200 bg-white p-4 shadow-sm"><textarea value={prompt} onChange={(event) => setPrompt(event.target.value)} placeholder="I need a deep clean for my 3 bedroom home…" className="min-h-28 w-full resize-none border-0 bg-transparent text-base leading-7 outline-none placeholder:text-stone-400" /><div className="flex justify-end"><button type="button" onClick={ask} className="grid h-11 w-11 place-items-center rounded-full bg-[#ff684c] text-white"><Send className="h-4 w-4" /></button></div></div><div className="mt-6"><strong className="text-sm">Popular requests</strong><div className="mt-3 flex flex-wrap gap-2">{QUICK_REQUESTS.map((chip) => <button type="button" key={chip} onClick={() => { setPrompt(chip); setAsked(true); }} className="rounded-full border border-stone-200 bg-white px-3 py-2 text-xs font-bold text-stone-600 hover:border-[#ffb3a3] hover:text-[#d95740]">{chip}</button>)}</div></div></>}</div>
        <footer className="border-t border-stone-100 bg-white/95 p-4 backdrop-blur"><button type="button" onClick={() => setPanelOpen(true)} className="flex w-full items-center justify-between rounded-2xl bg-[#ff684c] px-5 py-4 text-sm font-extrabold text-white shadow-[0_8px_22px_rgba(255,104,76,.28)]">Book Home Cleaning <ArrowRight className="h-5 w-5" /></button><p className="mt-2 text-center text-[10px] text-stone-400">Book any time — Madison is here if you have questions first.</p></footer></section></div></section>
    {panelOpen && <div className="fixed inset-0 z-20 bg-stone-950/20 backdrop-blur-[1px]" onClick={() => setPanelOpen(false)}><aside className="absolute inset-y-0 right-0 w-full max-w-[610px] overflow-y-auto bg-[#fffdfb] shadow-[-28px_0_70px_rgba(49,34,24,.20)]" onClick={(event) => event.stopPropagation()}><header className="sticky top-0 z-10 flex items-center justify-between border-b border-stone-200 bg-[#fffdfb]/95 px-6 py-5 backdrop-blur"><div><span className="text-[10px] font-extrabold tracking-[.18em] text-[#e85e45]">BOOK YOUR CLEANING</span><h2 className="mt-1 text-2xl font-black tracking-tight">Let&apos;s make it official.</h2></div><button type="button" aria-label="Close booking preview" onClick={() => setPanelOpen(false)} className="grid h-11 w-11 place-items-center rounded-xl border border-stone-200 text-stone-700"><X className="h-5 w-5" /></button></header><div className="px-6 py-6"><div className="flex items-center gap-2 text-xs font-bold"><span className="grid h-7 w-7 place-items-center rounded-full bg-[#ff684c] text-white">1</span><i className="h-px flex-1 bg-stone-200" /><span className="grid h-7 w-7 place-items-center rounded-full bg-stone-100 text-stone-500">2</span><i className="h-px flex-1 bg-stone-200" /><span className="grid h-7 w-7 place-items-center rounded-full bg-stone-100 text-stone-500">3</span></div><div className="mt-2 flex justify-between text-[10px] font-bold uppercase tracking-wider text-stone-500"><span>Details</span><span>Schedule</span><span>Review</span></div><section className="mt-8"><span className="text-[10px] font-extrabold tracking-[.16em] text-[#e85e45]">BUILD YOUR CLEANING</span><h3 className="mt-2 text-2xl font-black tracking-tight">Tell us about your home.</h3><p className="mt-2 text-sm text-stone-500">This is the existing booking flow, now separate from the guide.</p><label className="mt-7 block text-sm font-bold">Choose your service</label><div className="mt-3 grid grid-cols-3 gap-2">{["Standard", "Deep clean", "Move out"].map((service, index) => <button type="button" key={service} className={`rounded-2xl border p-3 text-left text-xs font-bold ${index === 1 ? "border-[#ff684c] bg-[#fff0ec] text-[#c84d37]" : "border-stone-200 bg-white text-stone-600"}`}><span className="block text-base">{index === 0 ? "✨" : index === 1 ? "🧼" : "📦"}</span><span className="mt-2 block">{service}</span></button>)}</div><div className="mt-6 grid grid-cols-2 gap-3"><RoomControl label="Bedrooms" value={bedrooms} minimum={0} maximum={7} onChange={setBedrooms} /><RoomControl label="Bathrooms" value={bathrooms} minimum={0} maximum={20} onChange={setBathrooms} /></div><label className="mt-6 block text-sm font-bold">Preferred appointment</label><div className="mt-3 grid grid-cols-3 gap-2">{["Tue 8", "Wed 9", "Thu 10"].map((date, index) => <button type="button" key={date} className={`rounded-2xl border p-3 text-center text-xs font-bold ${index === 1 ? "border-[#ff684c] bg-[#fff0ec] text-[#c84d37]" : "border-stone-200 bg-white text-stone-600"}`}><CalendarDays className="mx-auto mb-1 h-4 w-4" />{date}</button>)}</div><label className="mt-6 block text-sm font-bold">Service address</label><div className="mt-3 flex items-center gap-2 rounded-2xl border border-stone-200 bg-white px-4 py-3 text-sm text-stone-400"><MapPin className="h-4 w-4 text-[#ff684c]" />Enter your address</div><div className="mt-6 rounded-2xl bg-stone-900 p-4 text-white"><div className="flex items-center justify-between"><span className="text-xs font-bold">Your cleaning</span><strong className="text-lg">$349</strong></div><div className="mt-2 flex items-center gap-2 text-[11px] text-stone-300"><ShieldCheck className="h-4 w-4 text-emerald-400" />Card is saved securely. You are not charged today.</div></div><button type="button" className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl bg-[#ff684c] px-5 py-4 text-sm font-extrabold text-white">Continue to schedule <ChevronRight className="h-4 w-4" /></button><p className="mt-4 flex items-center justify-center gap-1 text-center text-[10px] text-stone-400"><MessageCircle className="h-3 w-3" />Preview only — no lead or booking is created.</p></section></div></aside></div>}</main>;
}
